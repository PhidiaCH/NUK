# ── 核心依賴 ──────────────────────────────────────────────────────────────────

# 音訊錄製（需要系統安裝 portaudio）
# Linux:  sudo apt install portaudio19-dev
# macOS:  brew install portaudio
pyaudio>=0.2.14

# 語音轉文字（Whisper）
openai-whisper>=20231117

# ── 說話者分離 ────────────────────────────────────────────────────────────────
# 需要接受 pyannote 使用條款並取得 HuggingFace token
# https://huggingface.co/pyannote/speaker-diarization-3.1
pyannote.audio>=3.1.0

# ── AI 會議記錄 ───────────────────────────────────────────────────────────────
# 需要設定環境變數 ANTHROPIC_API_KEY
anthropic>=0.40.0

# ── 終端 UI ───────────────────────────────────────────────────────────────────
rich>=13.7.0

# ── PDF 輸出 ──────────────────────────────────────────────────────────────────
# 內建 CJK CID 字型 (STSong-Light)，支援繁體中文，無需額外字型檔
reportlab>=4.1.0

# ── 音訊處理輔助（可選，提升相容性）────────────────────────────────────────────
soundfile>=0.12.1
